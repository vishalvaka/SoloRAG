# ───────── Testing ────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
httpx==0.27.0                # AsyncClient for FastAPI testing
aiosqlite==0.22.0            # In-memory SQLite for test DB

# ───────── Linting & formatting ──────
ruff==0.4.3
mypy==1.10.0

# ───────── Security scanning ─────────
bandit==1.7.8                # Python SAST (security issues in code)
safety==3.2.4                # Known vulnerability scanner for deps
pip-audit==2.7.3             # Audit installed packages for vulns

# ───────── Type stubs ────────────────
types-requests==2.32.0.20241016
